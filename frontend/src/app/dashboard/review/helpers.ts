/** Keyboard model of the review list (B-14). Shared with every other decision list. */
export { inboxKeyFor as reviewKeyFor, moveSelection, selectionAfterRemove } from "@/lib/inbox-keys";
export type { InboxKey as ReviewKey } from "@/lib/inbox-keys";
