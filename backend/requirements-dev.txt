# Developer + CI toolchain. Runtime deps live in requirements.txt.
# Installed by `make setup`; `make test` self-heals if any are missing.
ruff>=0.9
pytest>=8.3
pytest-asyncio>=0.25
pytest-cov>=6.0
pytest-env>=1.1.5
pre-commit>=4.0
