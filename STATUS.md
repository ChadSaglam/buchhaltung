# STATUS — Buchhaltung

> Generated by `make status` (`scripts/status.sh`) — do not edit by hand.
> 2026-09-11 · `main` @ `b304b78` · app version **2.0.0**

## Numbers

| Area | Count |
|------|-------|
| Backend tests (`def test_`) | 200 in 16 files |
| Frontend unit tests (vitest) | 44 in 4 files |
| E2E specs (Playwright) | 4 |
| API routers / routes | 17 / 51 |
| ORM models | 16 |
| Alembic migrations | 10 (head: `a400bdc46480`) |
| Roadmap B-xx open / done | 13 / 25 |

## Open roadmap items

| ID | Item | Sev / Effort |
|----|------|--------------|
| B-14 | Review queue: optimistic accept/reject with rollback; keyboard `j/k/a/r`. (Queue is on SWR since B-18 — `mutate(optimistic, { rollbackOnError: true })` is the whole change.) | M / M |
| B-16 | Dashboard KPIs auto-refresh (SWR `refreshInterval`), no reload. | L / S |
| B-21 | Replace remaining `err: any` in scanner/modell hooks with generated types (5 eslint warnings). | L / S |
| B-15 | Scan progress streamed (NDJSON already used by AI chat) instead of spinner. | M / M |
| B-34 | `classifier_models.model_sha256` column: record the digest of the packed model at training time and check it in `model_blob.unpack()` on top of the HMAC (detects silent corruption, lets an operator audit which model is live). Also: a "Modell neu trainieren" hint in the UI when the API logs an unsigned blob. | L / S |
| B-17 | Steuerberater export pack: Banana TSV + PDF summary + receipts zip, one click. | M / L |
| B-22 | Audit log surfaced in UI (model exists: `audit_log.py`). | M / M |
| B-23 | Usage limits enforced from `usage_event` (plan free/pro). | M / M |
| B-20 | Onboarding: first scan guided, sample receipt, Kontenplan import wizard. | M / M |
| B-24 | Postgres RLS (`SET LOCAL app.tenant_id`) as defence in depth — after platform contract. | H / L |
| B-25 | Backup/restore script + restore drill (models + DB). | H / M |
| B-27 | N+1 audit on bookings list + stats. | M / M |
| B-28 | Index audit: `(tenant_id, date)`, `(tenant_id, status)` on bookings. | M / S |

See `ROADMAP.md` for order and context.
